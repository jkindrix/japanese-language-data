{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jkindrix/japanese-language-data/schemas/pitch-accent.schema.json",
  "title": "Pitch accent",
  "description": "Pitch accent data for Japanese words, derived from the Kanjium accents.txt file. Each entry maps a word (in kanji+kana form) to its pitch accent mora positions. Accent positions are 0 for heiban (flat) or N where the accent falls after the Nth mora.",
  "schemaVersion": "0.8.0",
  "type": "object",
  "required": [
    "metadata",
    "entries"
  ],
  "additionalProperties": false,
  "properties": {
    "metadata": {
      "$ref": "#/$defs/metadata",
      "description": "Build metadata: source provenance, license, generation date, counts, field documentation, and attribution."
    },
    "entries": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/pitchEntry"
      },
      "description": "Array of data entries."
    }
  },
  "$defs": {
    "metadata": {
      "type": "object",
      "required": [
        "source",
        "license",
        "generated",
        "count",
        "field_notes"
      ],
      "properties": {
        "source": {
          "type": "string"
        },
        "source_url": {
          "type": "string",
          "format": "uri"
        },
        "license": {
          "type": "string"
        },
        "source_version": {
          "type": "string"
        },
        "generated": {
          "type": "string",
          "format": "date"
        },
        "count": {
          "type": "integer",
          "minimum": 0
        },
        "field_notes": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "attribution": {
          "type": "string"
        },
        "coverage_date": {
          "type": "string",
          "description": "Approximate date through which the upstream source has been maintained. Vocabulary added after this date is unlikely to have pitch accent data."
        },
        "warnings": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Data quality warnings (e.g., null mora_count rate)."
        },
        "note": {
          "type": "string",
          "description": "Additional context about the data source or extraction methodology."
        },
        "extraction_date": {
          "type": "string",
          "format": "date",
          "description": "Date when the data was extracted from the upstream source."
        },
        "wiktextract_dump_date": {
          "type": "string",
          "description": "Date of the Wiktionary dump processed by wiktextract."
        }
      },
      "additionalProperties": false
    },
    "pitchEntry": {
      "type": "object",
      "required": [
        "word",
        "reading",
        "pitch_positions"
      ],
      "properties": {
        "word": {
          "type": "string",
          "description": "Word spelling, typically containing kanji. Matches the first column of Kanjium's accents.txt."
        },
        "reading": {
          "type": "string",
          "description": "Kana-only reading of the word, used for pitch accent alignment."
        },
        "pitch_positions": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "integer",
            "minimum": 0
          },
          "description": "Pitch accent mora positions. 0 = heiban (flat, no drop). N = accent falls after mora N. Multiple values mean multiple accepted accent patterns exist."
        },
        "mora_count": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 1,
          "description": "Number of morae in the reading, for convenience."
        }
      },
      "additionalProperties": false
    }
  }
}
