# Runtime dependencies for the build pipeline.
# Pinned versions for reproducibility.
requests==2.32.3
jsonschema==4.23.0
genanki==0.13.1

# Development / test dependencies.
pytest==8.3.3
pytest-cov==6.1.1
ruff==0.9.10
