"""Test suite for the Japanese Language Data build pipeline.

Tests are independent of the data build: they run against the committed
schemas and small fixture data, not the full output. Use ``just test``
or ``pytest tests/``.
"""
